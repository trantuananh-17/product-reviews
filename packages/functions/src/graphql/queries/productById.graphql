query GetProductById($id: ID!) {
  product(id: $id) {
    id
    title
    featuredImage {
      id
      originalSrc
    }
    metafield(namespace: "reviews_product", key: "data") {
      id
      value
    }
  }
}
